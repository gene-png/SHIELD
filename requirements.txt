Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
Flask-WTF==1.2.2
Flask-Limiter==3.9.2
SQLAlchemy==2.0.41
alembic==1.14.0
psycopg[binary]==3.2.3
gunicorn==23.0.0
Werkzeug==3.1.6
WTForms==3.2.1
python-dotenv==1.2.2
authlib==1.6.11
requests==2.33.0
bleach==6.2.0
bcrypt==4.3.0
anthropic==0.49.0
redis==5.2.0
rq==2.0.0
openpyxl==3.1.5
pypdf==6.10.2
python-docx==1.1.2
PyYAML==6.0.2
itsdangerous==2.2.0
markupsafe==3.0.2

# PII redaction (defense-in-depth on the AI egress path).
# Presidio Analyzer wraps spaCy NER + pattern recognizers. The
# en_core_web_sm spaCy model is installed in docker/app.Dockerfile
# (spaCy resolves models by name at runtime; it's not a normal pypi dep).
# At ~12 MB, the small model is sufficient for PERSON/LOCATION/ORG NER on
# the kind of free-text that shows up in capability notes and uploads.
presidio-analyzer==2.2.354
spacy==3.7.5
